# Piper node.js test project

This project is used for end to end tests of

* executeBuild (via xMake)
* executeOnePageAcceptanceTests (`buildTool: 'npm'`)
* executeWhitesourceScan (`scanType: 'npm'`)
* advanced SAST PR voting
